# AI 评测集

`cases.json` 提供 12 个脱敏、自编覆盖场景的索引。真实模型接入后，评测脚本应记录：拆分准确率、固定首句保护率、评分证据完整率、结构修复次数及同一输入多次运行的分数波动。

当前仓库不伪造这些准确率。`tests/` 中的用例通过注入 Provider 验证确定性的产品契约——真实评测只依据本次输入、未配置模型时报错而非回退示例、示例报告只能由显式 demo 入口产生、段首句不被纠错也不计入原创词数、模型输出不合规时不展示编造报告。
