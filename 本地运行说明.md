# 在你的电脑上运行 EssayFlow

## 1. 装依赖

在这个文件夹里打开终端（把文件夹拖到终端窗口里可以直接得到路径）：

```bash
cd ~/Downloads/EssayFlow-v2   # 换成这个文件夹的实际位置
npm install
```

## 2. 配置模型（想用评测功能才需要）

在项目根目录新建 `.env.local` 文件，写入三行：

```
OPENAI_API_KEY=你的公司网关 key
OPENAI_BASE_URL=https://model.zhenguanyu.com/v1
OPENAI_MODEL=kimi-k2.6
```

`.env.local` 已经在 `.gitignore` 里，不会被提交。

不配也能跑，首页和示例报告都能看，只是点评测会明确提示"当前未配置 AI 模型"。

## 3. 启动

```bash
npm run dev
```

浏览器打开 http://localhost:3000

- `/` 是产品介绍首页
- `/evaluate` 是评测工作台
- `/evaluate?demo=1` 直接看脱敏示例报告

## 4. 提交代码到 GitHub

这份代码是在新分支上改的，还没推到远端。在项目目录里：

```bash
git checkout -b fix/real-evaluation-mock-isolation
git add -A
git commit -m "fix: 隔离真实评测与脱敏示例，重构首页与文档"
git push -u origin fix/real-evaluation-mock-isolation
```

推上去之后在 GitHub 网页上开 Pull Request，确认没问题再合并到 main。

## 常见问题

**`npm run dev` 报端口占用**：换端口 `npm run dev -- -p 3001`。

**评测一直转圈**：单次评测约 35—40 秒是正常的，模型要一次生成完整报告。

**评测报错"调用 AI 模型失败"**：检查 `.env.local` 里三个值有没有写错，尤其 `OPENAI_MODEL` 必须是网关自己的模型 ID（可以浏览器打开 `https://model.zhenguanyu.com/v1/models` 带上 key 查）。

## 其他命令

```bash
npm test        # 25 个测试
npm run lint
npm run typecheck
npm run build
```
