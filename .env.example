# EssayFlow 环境变量示例
# 复制为 .env.local 后填写。.env* 已被 .gitignore 忽略，切勿提交真实 Key。

# 必需：OpenAI 兼容接口的 API Key。仅服务端读取，不会下发到浏览器。
# 未配置时，真实评测会返回明确错误（HTTP 503），不会退回脱敏示例报告。
OPENAI_API_KEY=

# 可选：自建或第三方 OpenAI 兼容网关地址，例如 https://your-gateway.example.com/v1
# 留空则使用 OpenAI 官方地址。
OPENAI_BASE_URL=

# 可选：评测所用模型名。留空时默认 gpt-4.1-mini。
# 需要选择能稳定遵循 JSON 输出要求的模型；使用网关时填该网关自己的模型 ID
# （可先请求网关的 /v1/models 查看可用列表）。
# 一次完整评测约需 35—40 秒，部署时注意函数超时上限要留足余量。
OPENAI_MODEL=
